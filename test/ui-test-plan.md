# Epi UI test plan

The maintained plan has moved to [tests/test-plan.md](../tests/test-plan.md).

The `test-ui` skill and runner use that file. This pointer is retained for older links; do not add duplicate test cases here.
